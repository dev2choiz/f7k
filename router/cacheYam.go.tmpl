// This file is autogenerated.
package router

{{- $lengthImports := len .Imports }}
{{- $lengthData := len .Data }}

{{- if not (eq $lengthImports 0) }}
import (
{{- range .Imports}}
    "{{.}}"{{end}}
)
{{- end }}

func PrepareRouterYaml() error {

    {{- if not (eq $lengthData 0) }}
        r := router.Instance()
        {{- range $name, $route := .Data }}
        r.Routes["{{ $name }}"] = router.NewRoute()
        r.Routes["{{ $name }}"].
            SetMethods([]string{ {{- range $route.Methods }}"{{.}}", {{- end }}}).
            SetPackage("{{ $route.Package }}").
            SetAction("{{ $route.Action }}").
            SetName("{{ $route.Name }}").
            SetController("{{ $route.Controller }}").
            SetPath("{{ $route.Path }}")
        {{- end }}
    {{- end }}
    return nil
}
