// This file is autogenerated.
package overrider

{{- $lengthImp := len .Imports }}
{{- if not (eq $lengthImp 0) }}
import (
{{- range .Imports}}
    "{{.}}"{{end}}
)
{{- end }}

func PrepareOverriders() error {
    {{- range $name, $target := .Targets }}
       {{ $target }} = {{ (index $.Overrides $name).PackageName}}.{{ (index $.Overrides $name).FunctionName}}()
    {{- end }}
    return nil
}
